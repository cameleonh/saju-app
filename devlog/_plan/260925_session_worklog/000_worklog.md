# 2026-09-25 작업경과 — 인연 사진 풀, 커플 검산, 일일운세 미리보기

## 이번 세션에서 배포된 변경

| 커밋 | 내용 | 상태 |
| --- | --- | --- |
| `bc19a84` | 인연 패널 사진 풀 100장(Pexels 라이선스, 오행×성별 10장). 차트 기반 결정적 선택, SVG 폴백, 정적 allowlist 확장, `images/matches/pool/manifest.json` 출처 기록 | 라이브 배포 확인 |
| `67bb6bd` | 커플 결과에서 한쪽 시각 미상일 때 4전통 비교가 렌더를 중단시키던 문제(버튼 무반응) 수정. 시각 미상 안내 카드로 대체, 결과 렌더 실패 시 입력 화면 복귀+오류 표시 | 라이브 배포 확인 |
| `1395a32` | e2e 매트릭스 편입(`npm run test:e2e`). 브라우저 4환경(Chromium/WebKit × desktop/mobile)+자산 그리드, CI는 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` | CI success |
| `1e86d94` | 커플 오행을 공통 기둥 기준으로 집계(시주 미상 시 3주 비교), 빈 시각을 정오로 대체하지 않고 안내, 입력 폼 예시 생년월일·시각 제거, 경도·태양시 보정 없음 명시 | 라이브 배포 확인 |
| `f8fc7b1` | 지지 상호작용에 해(害)·파(破) 추가, 본인/상대 원국 내부 표식 분리 표시, 표시 유형(충·형·육합·해·파·완전 삼합) 공개 | 푸시 완료 · 배포 시점 AWS 타임아웃으로 라이브 미확인 |

## 미커밋 로컬 작업 (검증 완료, 커밋 대기)

- **일일운세 미리보기 개선**
  - 기준 명식 선택 드롭다운(`#daily-teaser-select`) — 저장된 단일 명식이 2개 이상일 때 노출
  - 최근 선택을 `localStorage['saju.daily-teaser-record']`에 기억하고 다음 방문 때 복원
  - "보기" → **"오늘의 운세 전체 보기"** 버튼으로 노출 강화, 스트립을 테두리 카드로 보완
  - `render()`가 입력 화면일 때 `syncFormInputsToState()`를 먼저 호출 — 백그라운드 갱신(계정 확인·출생지 로드)이 끼어들어도 타이핑 값 유실 방지
  - 서비스워커 캐시 `saju-app-shell-v36`
- **테스트**
  - smoke 259, onboarding 55 (입력값 보존·빈 시각 가드·기본값 제거·일일운세 선택/기억 검증 포함)
  - e2e S21 추가: 선택 노출, 선택 변경 반영, 새로고침 후 선택 지속
  - e2e 헬퍼에 출생지 목록 로드 대기(`waitForInputSettled`) 추가 — WebKit 입력 유실 플레이크 제거

## 검증 결과 (최종)

- `npm test`: 전체 통과
- `npm run test:e2e`: **chromium desktop 42/42 · webkit desktop 42/42 · chromium mobile 43/43 · webkit mobile 43/43** (E2E ALL GREEN)

## 특이사항 / 미해결

- `f8fc7b1` 푸시 직후 `saju.blog`가 외부에서 타임아웃되어 배포 반영을 확인하지 못했다. 이후 재확인 필요. `/health` 응답은 `adapter-only, durable:false`(영구 DB 없는 로컬 저장 구성)이며 이는 접속 장애가 아니다.
- 子 지장간은 학파 차이(癸 단독 표 vs 월률분야 癸+壬)가 있다. 앱은 월률분야 표를 유지하고 결과와 근거에 표기 기준을 공개한다. 6월 14일 실제 입력 기준 4주는 `癸亥·戊午·癸酉·乙卯 / 甲戌·己巳·庚子·미상`로 검산 일치.
- 관계 해석 문구는 계산 확정이 아니라 대화용 가설임을 명시(일간 오행 설명, 월간 십신 표기 정정 포함).
